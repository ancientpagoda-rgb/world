{
  "name": "world-smoke-guard",
  "private": true,
  "type": "module",
  "scripts": {
    "check:syntax": "node --check app.js",
    "check:static": "node scripts/static-guard.mjs",
    "test:setup": "playwright install chromium",
    "test:smoke": "node scripts/browser-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
